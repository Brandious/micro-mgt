import { Typography } from '@material-ui/core'
import { Box } from '@mui/material'

export const ProjectsDetail = (): JSX.Element => {
  return (
    <Box
      sx={{
        mt: 2
      }}
    >
      <Typography variant="h5">ProjectsDetail</Typography>
    </Box>
  )
}
